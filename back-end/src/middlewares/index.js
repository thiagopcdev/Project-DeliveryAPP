const userValidations = require('./users/userValidations');
const saleValidation = require('./sales/saleValidation');
const auth = require('./auth');

module.exports = {
  userValidations,
  saleValidation,
  auth,
};
